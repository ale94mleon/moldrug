[build-system]
requires = [
    "setuptools >= 54",
    "wheel",
    "setuptools_scm[toml]>=6.2"
]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "moldrug/_version.py"
version_scheme = "no-guess-dev"
local_scheme = "no-local-version"